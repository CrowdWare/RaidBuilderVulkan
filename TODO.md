# TODO
- Oberfläche für FileHistory um alte Version zu sichten und evtl zu restaurieren und History zu purgen.
- CMD-S ins Menu integrieren
- SMS als Library nutzen
- SMS als Glue zwischen SML und UI nutzen
    MenuItem { label:"Exit" onClick: requestClose }
    requestClose ist hier ein enum und verweist auf sms 
    fun requestClose() {}
- Action bar and inventory with icons, save actionbar
- Vulkan install into .app
- Walk above 1 Block should be able, with a little delay, making a big step
- Jump on 2 blocks is normal and jumping and pulling up on 3 blocks is also possible (idea stolen from Hytale)


## BlockBuilder
- In the editmode we could build blocks of 6 x 6 SubBlocks.
- Add full block, add pane, add cube
- extrude pane, duplicate selected
- paint face
- bake block while saving
- automatically add to inventory
- maybe also create block with higher resolution 12 * 12